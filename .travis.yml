language: node_js

node_js:
   - "4"
   - "6"
   - "8"
   - "9"
   - "10"
   - "12"   
   - "node"

services:
   - rabbitmq

before_install:
   - node --version | (grep  -v 'v5' && npm install -g npm@2) || true

sudo: false
